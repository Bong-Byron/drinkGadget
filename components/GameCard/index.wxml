<view class="game-card" bindtap="handleTap">
  <view class="icon-stage">
    <image class="card-icon-image" src="{{iconPath}}" mode="aspectFit" />
  </view>
  <view class="card-title">{{title}}</view>
  <view class="card-desc">{{desc}}</view>
</view>
